pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'pwd'
                sh 'go build'
            }
        }

        stage('Release') {
            steps {
                sh 'mv ./jenkins_test build/package/'
            }
        }
    }
}